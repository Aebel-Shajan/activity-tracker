import type { HeatmapSettings } from "./components/heatmap-visual";


export const DEFAULT_HEATMAP_SETTINGS: HeatmapSettings = {
  radius: 7,
  daySpacing: 2,
  monthSpacing: 20,
  xOffset: 0,
  yOffset: 0,
}